using UnityEngine;

public class WallScript : Structure
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        SetStructureType(StructureType.Wall);
    }
}
