using UnityEngine;

public class Core : Structure
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        SetStructureType(StructureType.Core);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
